#include "milk/scenegraph/irenderable.h"
#include "milk/scenegraph/cscenemanager.h"
using namespace milk;

/*
void IRenderable::onNewSceneManager(CSceneManager *pSceneManager)
{
	if(m_pSceneManager)
		m_pSceneManager->removeRenderable(this);
	if(pSceneManager)
		pSceneManager->addRenderable(this);
}
*/
