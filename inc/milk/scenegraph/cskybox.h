#ifndef MILK_CSKYBOX_H_
#define MILK_CSKYBOX_H_
/*
#include <string>
#include "milk/renderer/ctexture.h"
#include "milk/boost.h"

namespace milk
{
	/// Class for loading and drawing skyboxes
	class CSkyBox
	{
	public:
		void load(const std::string& dir, const std::string& end);
		bool loaded() const;
		void unload();

		void draw() const;

	private:
		//order: back, front, left, right, bottom, top;
		boost::shared_ptr<CTexture2D> m_textures[6];
	};
}*/

#endif
